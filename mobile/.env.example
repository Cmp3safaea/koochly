# Origin of your Next.js app (no trailing slash). Required on a real phone/tablet
# so the app can load /api/ads/priority — use your machine's LAN IP, not localhost.
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:3000
