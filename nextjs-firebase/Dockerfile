# syntax=docker/dockerfile:1
#
# Build context must be the monorepo root, e.g.:
#   docker build -f nextjs-firebase/Dockerfile .
#
# Canonical Dockerfile: ../Dockerfile (repo root) — kept in sync via `include`.
include ../Dockerfile
