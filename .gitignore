# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log

# Local env at repo root (if you add one)
.env
.env.local
.env.*.local

# Never use blanket *.json — it prevents committing package.json / lockfiles and breaks Docker + Cloud Build.
# Ignore common credential / dump JSON instead:
**/firebase-adminsdk*.json
**/*-service-account*.json

# Dependencies
node_modules/

# Expo / React Native (mobile/)
mobile/.expo/
mobile/dist/

# Optional: IDE / tooling (uncomment if you prefer not to commit)
# .idea/
# *.swp
# *~
